# eiviz

## このプロジェクトについて

このプロジェクトは、次世代の映像スイッチャーソフトウェアeivizのレポジトリです。
vMix, OBS-Studio, Panasonic Kairos, Viz/NewTek TriCaster のようなソフトウェアベースのスイッチャーの良いところ・反省点を吸収し、現代の技術で再構成されたソフトウェアベースの多用途ビジョンミキサーです。
コア部分はオープンソース/Source Availableで開発されており、GitHub上に公開されています。
レポジトリは以下です。
https://github.com/MikanseiLaboratory/eiviz

### キーとなる技術要素

本プロジェクトでは、高いパフォーマンスを保ちつつクロスプラットフォームに対応するため、以下の技術が採用されています。

- Rust + WGPU 30
  - コア機能と映像処理を担います。しばしば「コア」「Mixer」と呼称されます。出来る限り映像をGPUの内部に閉じ込めることで、CPU<->GPU間の転送を削り高いパフォーマンスを目指しています。
  - C ABIでメインで使う関数を開放し、`cdylib`としてビルドすることでクロスプラットフォームを実現しています。
  - パフォーマンスクリティカルな処理は、OS間の抽象化やWGPUのHALを剥がし、内部のDX12やMetalなどに直接アクセスすることでパフォーマンスの向上を図っています。
  - 特にGPUテキスチャをUIに表示する機能は、1920x1080の高画質なデータを毎秒60枚スムーズに転送されることが求められるため、可能な限りボトルネックを排除しプラットフォーム毎の最適化を使用する必要があります。
  - しかし、クロスプラットフォームの保守性を優先するため、traitなどの抽象化は正しく使い、Rustのゼロコスト抽象化は出来る限り活用する必要があります。
- UI部分/ホスト部
  - GUI/操作面は「ホスト」や「UI」と呼ばれており、OSごとに`hosts/`へ分かれています。
  - Windowsは`hosts/win32`（C# 14 + WPF）、macOSは`hosts/macos`（Swift 6 + SwiftUI）、Linuxは`hosts/linux`（開発中。Rust、gtk4-rsとVulkanを予定）。
  - また、現在未実装ですがheadless機能があり、UIを完全にオフした状態で走らせることも可能です。
  - 映像合成に関する処理や、操作・表示 に関係しない処理をここにおいてはいけません。headlessモードや分離に影響が出てパフォーマンスが悪化する恐れがある為です。
    - Windows
      - DX12で実装されています。 将来的にVulkan APIも検討していますが、Linuxサポートの副産物でありメインストリームではありません。
      - 過去のなごりで`CommandQueue`というものでFFI操作を直列化していますが、これは不要な実装だったため、新規に実装する制御はこれをバイパスしても構いません。
    - macOS
      - Metalを活用しています。 Windowsと近い形での実装を狙い、UI/UXも出来るだけ同一になるように実装してください。
- その他
  - core部分とホスト部はセットでリリースされるため、C ABIの関数定義が変わった場合でも、全プラットフォームの実装が一致していれば後方互換性について考える必要はありません。
  - 本アプリの公式対応言語は日本語、英語とします。 ドキュメントはコメントは英語でも構いませんが、PRの概要やTest Planは日本語で記述してください。
  - 本アプリはvMix, OBS, TriCasterのような高クオリティなソフトウェアの代替を今すぐ狙うものではありません。まずはコア部分を公開した状態で開発することでコミュニティに学習リソースを提示し、既存のソフトウェアスイッチャーに実装の可能性と機能を提示するためのテストプロジェクトです。
  - プロジェクトは現在ベータ版です。将来安定した際には製品化も検討しています。その場合、プロ機能はeiviz-proと呼ばれる非公開のレポジトリに格納されているクレートの機能を使用し、これはRustの`features`で管理されます。 コア機能はこれまで通り公開されます。

## eivizと他ソフトウェアスイッチャーの際

eivizは上記のソフトウェア群から影響を受けており、共通の概念が一部登場します。

- Mixing Unit
  - 他のスイッチャーで言うMix Effect相当の機能です。 ただし、全MUをフラットに扱うため、イメージとして一番近いのはPanasonic Kairosです。
  - シーンのスイッチング制御はここで実現します。このMUからの出力がeivizからの出力ということになります。
- Scene
  - OBSでのシーンに相当するものです。複数の映像入力を配置することで一つの映像合成を実現します。
  - Mixing Unitは、このSceneを切り替えたり、オーバーレイとして載せることで映像合成オペレーションを行います。
- Input
  - OBSでのSourceに該当するものです。映像入力の本体であり、これには色ソース、USBカメラやNDI/OMTなどのキャプチャーやMixing Unitの映像出力のフレームバッファ付き再ルートも含まれます。
  - 映像合成時の一番低い(入口に近い) 要素です。これらInputを組み合わせることで映像合成を実現します。

# その他

- 現在のメイン開発ターゲットはReBARに対応したWindows x64 + DX12環境です。 サブターゲットとして、ReBAR非対応のWindowsと、macOSがあります。
- Apple Silicon搭載のmacOSでは非常にパフォーマンスが向上するポテンシャルを秘めています。しかし、現在開発環境にApple Silicon搭載Macが存在せず、Intel CPU搭載のMacbook Air (2019)のみで動作確認を行っています。　スペックが低くビルド能力にも乏しいため、GitHub Actions上でビルドしたAlpha版をMacbookでダウンロードすることでテストを実施しています。
- 
- 一部のクレート(openmediatransport-rs, vmix-rsなど)は同じ開発者がメンテナンスするクレートです。新機能や不具合修正が必要な場合、ブランチを切って修正し、そのバージョンに切り替えることも可能です。
- 現在映像処理の抽象化と、音回りの安定化が難航しています。新規の実装内容はこちらに留意してください。
- 映像の転送処理が大きなボトルネックとなります。出来る限り各プラットフォームごとの最適化を利用し、それが最適な形で抽象化されているか意識するようにしてください。 例えば、mp4の伝送はDXGIを使うかCPUでフレームを転送するかで数倍のパフォーマンスの差異が発生します。
- 開発はFlowingSPDGがメンテナを担当しています。映像スイッチャーとしての設計、ソフトウェアとしてのアーキテクチャはある程度責任を持って設計・開発していますが、AIを使い開発速度を優先した開発を行っているため、一部不適切な実装が存在する可能性もあります。 将来的のことも考え、1人のメンテナでも長く保守することが出来るコードを書くように意識してください。
- コーディングにはゲームや映像処理のような映像・GPU処理の知識と、vMixやKairosのような映像スイッチャーの知見が求められます。知識や実装に自信がない場合は念入りに調査を実施し、不明な点や曖昧な点は放置せず、開発時に必ず確認するようにしてください。