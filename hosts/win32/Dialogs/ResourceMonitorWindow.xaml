<Window x:Class="Eiviz.Host.Dialogs.ResourceMonitorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Resource Monitor" Height="480" Width="820"
        Background="#2B2B2B" Foreground="#EEEEEE">
    <DockPanel Margin="12">
        <TextBlock x:Name="SummaryText" DockPanel.Dock="Top" Margin="0,0,0,8" Foreground="#9CCC65"/>
        <ListView x:Name="UsageList" Background="#222" Foreground="#EEE" BorderBrush="#444">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Name" Width="180" DisplayMemberBinding="{Binding Name}"/>
                    <GridViewColumn Header="Kind" Width="80" DisplayMemberBinding="{Binding Kind}"/>
                    <GridViewColumn Header="Size" Width="100" DisplayMemberBinding="{Binding Size}"/>
                    <GridViewColumn Header="CPU" Width="70" DisplayMemberBinding="{Binding Cpu}"/>
                    <GridViewColumn Header="GPU" Width="70" DisplayMemberBinding="{Binding Gpu}"/>
                    <GridViewColumn Header="RAM" Width="80" DisplayMemberBinding="{Binding Ram}"/>
                    <GridViewColumn Header="VRAM" Width="80" DisplayMemberBinding="{Binding Vram}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </DockPanel>
</Window>
